# Linked List

## Advantages:
  
  + Easy and fast insertion and removal
  + Memory could be dynamically allocated

## Disadvantage:

  + By nature is a sequential structure, so look up is going to be slow, has to do sequence scan
  + Random accessing is also hard
  + Additional resources needed for storing the pointers